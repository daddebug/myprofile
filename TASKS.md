# Tasks

## Unresolved

- **Resolved 2026-09-20: the previously recorded typecheck/build blocker is obsolete.** `pnpm typecheck` and the full `pnpm build` now pass after the Portfolio 2.0 final template cutover.
- **Portfolio 2.0 cutover complete; retain archives for manual reference only.** Active paths accept exactly five templates. Do not reconnect `.local-backups/retired-template-archive/`, old browser drafts, legacy migration backups, or orphaned asset mappings to hydration/rendering. Manual content redesign from archives remains optional future work.
- **Homepage 3.0 continuous-3D-ambient-scene direction abandoned outright (2026-09-19) — do not resurrect without explicit new instruction.** Two full rounds were built (round 1: a round `CatmullRomCurve3` tube, rejected as a "black snake / giant loop"; round 2: a flattened-ellipse ribbon authored in per-state viewport-fraction control points, measured against explicit composition rules) before the user replaced the whole direction with a modular, phased rebuild (`HomeIntroTransition`/`HomeHero`/`HomeProjectFlow`/etc., see the entry below) that has NO decorative ambient WebGL layer on the Homepage at all. `src/pages/HomeAmbientScene.tsx` and `src/pages/webglSupport.ts` have been deleted, not just unused. `three`/`@react-three/fiber` remain installed dependencies (unused for now) since the new Phase D/E of the modular rebuild will need WebGL again for the project-cover distortion bridge — do not uninstall them without checking that plan first.
- **Homepage 3.0 Modular Interaction Redesign — Phase A approved; Phase B complete, awaiting approval before Phase C (2026-09-19) — do not continue past this gate.** Phase A (opening aperture + Hero extraction) shipped and was accepted structurally. Phase B ("static project composition, different cover sizes, scalable ordered portfolio flow" only — explicitly NOT Elastic Grid Scroll/WebGL/shader distortion/Haoqi hover/GSAP Flip route transition) is now also implemented: new `src/pages/HomeProjectFlow.tsx` + `home-project-flow.css` replace the old equal 3x2/2-column `.home-object` grid and its slot-picker editor entirely. **Data-architecture change, confirmed with the user via AskUserQuestion before implementing**: the Homepage's project flow now reads directly from the canonical project catalog (`useProjectCatalog`, same archive-order/visibility rule `ProjectEndSections.tsx`'s "Other Projects" pool already uses) instead of the manually-bound `homeProjectSlots`/`homeExplorationSlots` two-list system — a new published project needs zero Homepage-side steps, it just appears in its own archive-order position. `homeProjectSlots.ts`/`homeExplorationSlots.ts` and their stored data are untouched (not deleted, non-destructive), but **`/work`'s "homepage placement" slot editor (`WorkPage.tsx`, still imports/uses these hooks) is now disconnected from what the Homepage actually renders** — flagging this clearly since it's a real, visible side effect of the confirmed architecture change, not something to silently paper over. `HomeContent.showProjectSection`/`showExploreSection` (the old PROJECT/EXPLORE visibility toggle) are also now unused — the toggle UI was removed from `HomeHero.tsx` since there's no more PROJECT/EXPLORE categorization to toggle, but the stored schema fields themselves were left in place, not deleted. Layout sizing is a deterministic 6-long repeating grammar (`LAYOUT_GRAMMAR` in `HomeProjectFlow.tsx`: large/small/medium/large/small/medium, index-derived via `archiveOrder` position, never randomized) matching the user's own worked example. Cards are static (title + optional/subtle year only; no tags/description/hover-reveal — explicitly reserved for Phase E). Editing Mode's cover-upload control overlays the existing image (reuses the exact `stageProjectCover`/`decodeProjectCover`/`commitProjectCover` pipeline `/work`'s own row editor uses) without changing card geometry. Verified live: unequal sizes obvious across 11 real published projects (well past one 6-item grammar cycle, confirming stability at scale), 0 measured overlaps (`getBoundingClientRect` pairwise check), no horizontal overflow, Editing Mode shows the same geometry as public view with only the cover-upload overlay added, mobile (375x812) single-column with its own aspect-ratio/spacing rhythm and no overflow. See `PROJECT_STATUS.md` for the full technical account. Do not build Phase C (Elastic Grid Scroll) or any later phase until the user reviews the Phase B screenshots and explicitly approves continuing.

- **19 more real project sections need manual rewriting (removed 2026-09-18 as part of the image-row exactly-2-images cleanup, by explicit instruction).** These were 3-6 item `image-row` instances across `interaction-intelligence-system`, `ai-assisted-ui-environment-design`, `ui-16ienxj`, `project-1ua2677`, `game-jam-8lzejf`, `3d-vdr4qg`, `ai-agent-6vaadf`, `project-1hp5k4t`, `project-wlu6qq`, and `project-y4coub` (9 of the 11 projects already affected by the entry below) where every item had real authored text forming an explicit comparison set, named classification, or step sequence -- splitting into separate `universal-media` instances would have broken the intended "read together" argument. Every original instance's full content (including all real image references) is preserved in `backups/legacy-template-migration/2026-09-18-image-row-backup.json`, with the full reasoning per instance in `2026-09-18-image-row-report.json` -- use these to manually rebuild each as one or more `image-row`(exactly 2)/`universal-media`/`statement-longform`/`direction-compare` instances, or a future new active template, at your own pace. `project-y4coub` lost the most (all 3 of its image-row sections: a 6-competitor analysis, a 3-part usage-pattern analysis, and a 4-solution proposal set).
- **27 real project sections need manual rewriting (removed 2026-09-18 as part of the legacy-template migration, by explicit instruction, not an accident).** `phase-milestones`/`process-flow`/`decision-table`/`xmind-breakdown` instances across `interaction-intelligence-system`, `ai-assisted-ui-environment-design`, `ui-16ienxj`, `project-1ua2677`, `game-jam-8lzejf`, `3d-vdr4qg`, `ai-agent-6vaadf`, `project-1hp5k4t`, `project-1ied3i`, `project-wlu6qq`, and `project-y4coub` were removed because their real structure (numbered step sequences, multi-column tables, mind-map trees) cannot losslessly become one of Portfolio 2.0's 5 active templates. Every original instance's full content is preserved in `backups/legacy-template-migration/2026-09-18-legacy-instance-backup.json`, with a per-instance account in `2026-09-18-migration-report.json` -- use these to manually rewrite each section using `statement-longform`/`image-row`/`universal-media`/`direction-compare`/`supporting-note` (or a future new active template) at your own pace. `interaction-intelligence-system` lost the most (4 of 11 real sections: phase-milestones, xmind-breakdown, process-flow, one of two decision-tables removed -- the other decision-table plus 2 circle-summaries were migrated, not removed).

- **Resolved (2026-08-26): Cover/TOC wide-page composition regression.** Width unification had correctly stopped global page scaling but left the fixed 1440px Cover/TOC composition centered inside wider final pages. Cover/TOC now render natively from their 1440px authored baseline at the final target width using one shared scale for all visuals and link rectangles. Project capture geometry remains untouched and unscaled. Real 1920px `/zh/export` and postflight passed.

- **Game-cover/UI-Practice export corruption (2026-08-17 incident) has a durable safety net but no confirmed root cause.** A real `EXPORT FOR PUBLISH` produced ~3-byte payloads for all 33 `game-experience-covers` and both `ui-practice-images` bundle images, which began overwriting real published files during `--confirm` before being caught and reverted (see `CHANGELOG.md`). `scripts/assetIntegrity.mjs` + the preflight/disk-fallback gate now blocks this class of corruption before any write. A live-instrumented re-diagnosis (IndexedDB → fetch → arrayBuffer → base64, per asset) the same day could **not reproduce** the corruption — every stage returned correct, full-size, correctly-signed bytes. Treat the original incident as environment/transient until it recurs; if it does, the gate will block it and the next diagnosis should compare against this session's clean trace.
- **`game-experience-covers` has no server-side disk-fallback** (unlike `project-covers`/`dynamic-template-images`/`project-body-indexeddb-assets`/`playable-game-covers`, which all resolve via `resolveAlreadyPublishedProjectBodyAsset`). Currently masked by `GameExperienceRecord.presentation.coverPublicPath` already being resolved on the record itself, so `import-production-bundle.mjs`'s own leniency (`!presentation.coverPublicPath` check) covers most real cases — but `buildPublishingPreflight`'s `collectContentTree` still requires a bundle image for every `coverAssetId`/`detectedCoverAssetId` reference with no fallback path, discovered while fixing `scripts/import-production-bundle-confirm-gate.test.mjs`'s "ready" scenario. Not fixed — out of scope for the integrity-gate task; would need its own disk-fallback branch analogous to the existing one.

- **Active: canonical sync must deploy current website implementation, not only imported data/assets.** Production `/zh/play` is still serving the pre-masonry Game Experience bundle because `portfolio:launcher-publish` builds the current worktree but stages only `publishedPortfolio.json`, `uiPracticeMetadata.json`, and `public/images/published/**`. Fix the canonical workflow, add stale-build verification, publish, and verify the real production page before resolving.

- **Publishing preflight blocks on one `project-body-indexeddb-assets` reference: `image-d3537b36-6223-484f-98a2-01a5131d70cb` (project `project-1ua2677`'s `fallbackImage`).** Surfaced 2026-08-10 in the same preflight run as the now-fixed `game-experience-covers` block, but is a different asset class and was explicitly out of scope for that fix — not yet investigated. Worth checking whether it's the same "already-published, not in local IndexedDB" pattern before assuming the asset is genuinely lost.
- **PDF viewer performance still unverified.** Not measured against a real generated PDF since the architecture/registry work landed.
- **Project trailing blank space resolved and literally verified (2026-08-14).** The canonical exact-web renderer now derives every project page height from settled visible text/media bounds plus 32px, and Collection export no longer auto-substitutes historical project override PDFs. A fresh real `/zh/export` download with four projects produced four equal-width continuous project pages, each reporting exactly 32px trailing space and visually ending at its real content.
- **Resolved (2026-08-09): TOC horizontal safe margins item was moot, and the registry has now been corrected.** The cover/index went through two redesigns (2026-08-08: dot+label strip → project cover cards; 2026-08-09: split into a clean identity-only cover page + a separate editorial index page) — there is no longer a per-slot dot/label TOC to overflow. `src/lib/pdf/pdfExportRegistry.json`'s `sharedGeometry`/`cover`/`toc` entries were updated to describe the current two-page structure and the new index column geometry instead of the stale 7-slot grid. `scripts/pdf-export-postflight-lib.mjs`'s TOC safe-area check already degrades safely (null-guarded on `sharedGeometry.tocGeometry`, which no longer exists) — it simply no-ops now rather than needing its own fix.
- **Project-page blank-segment pagination bug: real root cause still unknown; only stopgaps are in place.** An explicit, opt-in-only `?emergencyPdfExport=1` override on `/export` now exists (`isEmergencyPdfExport()` in `src/lib/portfolioCollectionExport.ts`, threaded to `captureProjectPage` in `scripts/portfolioCollectionExportPlugin.ts`): when a segment-count mismatch still occurs even with `maxSegmentHeightPx=16000` (real repro 2026-08-07: `project-1ua2677` "2 segments for 1 planned" on a later run), it keeps every generated segment instead of failing, embedding any segment beyond the planned count at its own full native height instead of cropping — verified in a real regenerated PDF, no clipping. Every other blocking check (missing draft, missing images, clipped content, failed rendering, empty body, template overflow) is untouched and still throws normally with or without the flag. Normal production exports never set this param. Real repro this session: `project-1ua2677` produced 4 physical PDF segments for 3 planned slices, then (with all projects selected) `interaction-intelligence-system` produced 5 for 4 — confirming this is systemic, not project-specific, and not caused by the `data-project-print`-scoped `figure/table/img` rule previously suspected (that attribute is provably never set during collection capture — its only setter is the owner-only manual print button, gated on a `?printProject=1` query param the collection pipeline never sends). Two CSS-only fixes were tried and both reproduced the identical failure: (a) forcing `break-inside: auto` on figure/table/img under `data-collection-export-capture`, (b) restoring `break-inside: avoid` on every top-level `[data-template-instance-id]`/`[data-document-section]` module (both since reverted/superseded — see `src/styles.css`, "EMERGENCY FIX" comment near `maxSegmentHeightPx`). This means **break-inside CSS is not the actual lever** — the real cause is most likely `page.pdf()`'s print-layout pass reflowing slightly taller than the on-screen measurement that produced `captureHeight`, an engine-level measurement/render mismatch, not yet root-caused. **Stopgap in place**: `maxSegmentHeightPx` in `scripts/portfolioCollectionExportPlugin.ts` raised from 3000 to 16000, so most real projects need only 1 segment (segmentCount=1 has no internal auto-pagination, sidestepping the bug class entirely for typical content). Verified against real generated PDFs for both previously-failing projects. **Any project tall enough to still need ≥2 segments (captureHeight > 16000px) remains exposed to this exact bug** — root-cause it before raising `maxSegmentHeightPx` further, since 19200px (200in) is Chromium's real ceiling.
- **The item above may now be entirely stale — needs its own verification pass, not corrected here.** Fixing the unrelated 2026-08-08 continuous-vs-section-pages splitting bug (see `CHANGELOG.md`) required reading the current `captureProjectPage` in `scripts/portfolioCollectionExportPlugin.ts` end to end: it delegates entirely to `window.__exactWebExport.buildSnapshot()` + `renderExactWebPdf()` now. Neither `maxSegmentHeightPx`, `segmentCount`, nor `isEmergencyPdfExport()` exist anywhere in that file or in `src/lib/portfolioCollectionExport.ts` anymore (confirmed via grep, not assumed) — the segment-based mechanism the item above describes appears to have been fully superseded. `src/lib/pdf/pdfExportRegistry.json`'s `dynamic-project`/`ui-personal-practice` entries still describe the old mechanism too (and cite `maxSegmentHeightPx=3000`, not `16000`) and may also need updating. Do not assume the blank-segment bug is still live, or still fixed by the stopgap described above, without re-checking against current code first.
- **`playable-game` and `figma-showcase` templates' actual PDF-capture behavior is an open verification gap**, flagged in `src/lib/pdf/pdfExportRegistry.json`'s `templates` array — not yet read/confirmed in this codebase, do not assume parity with `figma-prototype`'s print-swap pattern.
- **"Personal Profile & Experience" Collection export section toggle has no effect.** `SECTION_ID_TO_COLLECTION_SECTION` in `PortfolioPdfBuilderPage.tsx` has no mapping for the `profile` section id, so enabling/disabling it in the 章节 tab silently does nothing to the generated PDF. Flagged 2026-08-08 while fixing the `DEFAULT_SECTION_ORDER` state-flow bug (see `CHANGELOG.md`); not fixed, since it wasn't the reported symptom and deciding what this toggle *should* do (add a real profile page? remove the toggle?) is a product decision, not a bug fix.
- **Collection export always requires at least one individually-checked project.** `runPortfolioCollectionExport` throws "没有选中任何项目" whenever zero projects are checked in the 项目 tab, even if the user only wants a cover-only or UI-Works-only export with intentionally zero projects. Flagged 2026-08-08 alongside the above; not fixed, out of scope for that fix.
- **Reported: Portfolio PDF Builder (`/export`) showing mixed Chinese/English.** Investigated 2026-08-08; **no code regression found**. `/zh/export` and `/en/export` were both live-tested and resolve locale correctly end-to-end (route param → `LocaleProvider` → `useLocale()` → every UI string in `PortfolioPdfBuilderPage.tsx`); the per-locale `localStorage` PDF config (`portfolioPdf.ts`) is correctly scoped and validated per locale; a bare `/export` (no locale segment) redirects to the home page, not to `/en/export`. The mixed appearance on `/en/export` is the *designed* fallback (English UI, and titles fall back to `titleZh` for the 4 projects with no `titleEn`) — matches spec, not a bug. Most likely cause of landing on `/en/export` unexpectedly: the browser's last-visited page before opening the builder was on an `/en/...` route (this session's own automated testing repeatedly visited `/en/work/...` URLs in the real browser profile while investigating the `ui-16ienxj` export — flagged as the probable proximate cause, not a code defect). **One real but currently dormant asymmetry found** while auditing the fallback order: `resolveProjectCatalog()` in `src/lib/projectMetadata.ts` (title/summary/category, ~lines 385-397) falls back `titleEn → titleZh` when `en` is empty, but the `zh` branch has no fallback to `titleEn` at all if `titleZh` were ever empty. No current project has an empty `titleZh`, so this produces no visible symptom today — a one-line parity fix (`locale === "zh" ? merged.titleZh.trim() || merged.titleEn : ...`) would close the gap if desired, but was not applied since it isn't the reported symptom's cause.

- **Collection PDF size target resolved (2026-08-09): 87.76MB → 5.47MB (93.8% reduction), verified end to end through the live `/export` UI.** Two-stage fix: (1) `scripts/exportImageResize.ts` right-sizes oversized images upstream, inside the disposable capture page, before `page.pdf()`; (2) `scripts/pdfStreamEncodingOptimizer.ts` closes the remaining Chromium raw-FlateDecode encoding gap with a rule reverse-engineered from a real Smallpdf-compressed reference of this portfolio (alpha images stay lossless-predictor; non-alpha images get a fixed-quality JPEG only when genuinely smaller). See `PROJECT_STATUS.md`'s "Collection PDF size" bullets and `CHANGELOG.md` for the full verified account. The earlier exploratory post-hoc PDF-object rewriter (`scripts/pdfImageOptimizer.mjs`, `scripts/pdfSizeOptimizeCli.mjs`) remains superseded and marked EXPERIMENTAL/UNUSED — not wired into any live pipeline, kept only for reference (documents a real confirmed `sharp` palette-PNG encoding bug).

## Pending verification

- Open `portfolio-static-zh-2026-08-08T13-07-13-925Z.html` in the user's real local-file browser context and visually verify homepage card parity, image quality, plus the full project 1 → home → project 2 → home → project 3 → home sequence. Structural comparison already passes: 8.89 MB; 66 unique embedded WebP payloads with no duplicate copies; no HomePlayExperience/unselected assets; UI Practice 21/21 images; Interaction Intelligence 9/9 images and 3/3 templates; AI-assisted UI 34/34 images and 6/6 templates; every captured project route main is visibly normalized. Do not move this to Resolved until the literal `file://` visual check passes.
- DILIDA DESK's one-click browser export bridge and canonical preflight are verified through the formal EXE (`0 NEW / 0 UPDATED / 30 UNCHANGED / 0 BLOCKED`). The confirmation/publish half was intentionally not run in that acceptance pass; verify commit scope, push, deployment readiness and live production only when the user explicitly confirms an actual publish.

- **Resolved (2026-08-14): canonical Collection PDF architecture and literal postflight.** Project snapshots now contain current project DOM only; one shared compositor measures content, adds independent vector page chrome, and preserves one continuous page without historical overrides or project-ID branches. Figma uses explicit `live|pdf|offline` mode and never instantiates an iframe in PDF/offline capture. Three real `/zh/export` PDFs passed postflight and visual inspection with equal 1440px project widths, 32px separation, 96px chrome, 24px final margins, visible Figma fallback, and working bidirectional index links.

## Deferred

(none)

## Resolved

- **`PortfolioTrackTabs.tsx`/`WorkPage.tsx` `pnpm typecheck`/`vite build` regression, actually fixed and verified this time (resolved 2026-09-18).** Root cause (not the same as the 2026-09-13 note below, which only patched the symptom): `PortfolioTrackTabs.tsx` imported two names that don't exist anywhere in the current `src/lib/projectMetadata.ts` (`PORTFOLIO_TRACK_LABELS` as an exported `{zh,en}` map, `PORTFOLIO_TRACK_ORDER`) -- retired names from an earlier iteration of the feature. Updated the consumer to the canonical, already-in-use API (`getPortfolioTrackLabel()`, `PORTFOLIO_TRACK_OPTIONS` -- the same pattern `ProjectQuickSettings.tsx`/`WorkPage.tsx`'s own track `<select>` already use). Separately removed one line of dead code in `WorkPage.tsx` (`activeTrack === "all" ||`) -- `usePortfolioTrack()`'s `activeTrack` type has no `"all"` state and nothing else in the codebase ever produces one. No Portfolio Track redesign. `pnpm typecheck` and the full `pnpm build` (tsc -b + vite build + production-privacy check) both pass. See `CHANGELOG.md`'s second 2026-09-18 entry for the full account.
- **Project Editing UX layered into Owner Permission / Editing Mode / Content Editing / Metadata / Publishing Settings (resolved 2026-09-13).** Content editing auto-activates with global Editing Mode (no more "EDIT CONTENT" click); Project Info trimmed to Project ID + title + description + year + category, with Slug/Visibility moved to a collapsed Advanced section; Portfolio Track/Publication Status/Theme Color/Tags moved to a new live-write Quick Settings popover (`ProjectQuickSettings.tsx`); Role/Tools/Collaborators have no editor UI anywhere this round (explicitly deferred to a future Project Overview surface); Archive Position/Featured on Homepage were never duplicated here -- WorkPage.tsx's archive order editor stays their one home. Added a small cover-editing affordance to Homepage cards in Editing Mode. One judgment call made without an explicit spec: `visibility` (public/hidden) wasn't named in either the "keep" or "remove" list for Project Info's main form -- moved it into the same Advanced section as Slug (not deleted, not left in the main form) since it's a Publishing/Display Settings concern per the round's own stated layering, not Project Metadata. Flag this if a different placement was intended. No schema/data field was removed. See `CHANGELOG.md` for the full verified account.
- **Other Projects manual 3-slot recommendation replaced with an auto-populated carousel (resolved 2026-09-12).** No more binding 3 slots by hand or seeing an empty/disabled card for an unbound slot. `ProjectEndSections.tsx` now derives an ordered pool from the live catalog (current project excluded, sorted by `archiveOrder`), filtered to public+published for anyone who isn't the owner, and cycles a fixed 3-card window one project at a time every 4 seconds (`pool[(index+offset) % pool.length]`, which also covers the 2-item and 1-item degenerate cases with no extra branching; a 0-item pool renders no section at all). Autoplay is `IntersectionObserver`-gated (only runs on screen) and pauses on hover. Deleted the now-fully-unused `src/lib/otherProjectsSlots.ts` and its wiring in `DynamicProjectPage.tsx` after confirming via repo-wide search it had no other consumer. See `PROJECT_STATUS.md`'s "Other Projects: auto-populated recommendation carousel" section and `CHANGELOG.md` for the full verified account.
- **Owner permission and Editing Mode split into two independent states (resolved 2026-09-12).** Previously `useOwnerMode()` controlled both project-click permission and all editor-chrome visibility, so `ProductionExportDock`/the Owner-Visitor pill rendered on every DEV homepage load. Added a separate `useEditingMode()` (`src/hooks/useEditingMode.ts`), defaulting to `false` even for the owner, gating `ProductionExportDock`'s contents, `OwnerVisitorToggle`, `InlineTemplateField`, homepage and "Other Projects" slot pickers, `CaseStudyEditorDock`/"EDIT CONTENT", and the `/work` and `/play` management toolbars — all rewired to `isOwner && editingMode`. Click-permission checks (`resolveHomeSlot`, `ProjectEndSections`' `resolveSlot`) stay on `isOwner` alone. A single small top-right pencil icon (owner-only, `data-edit-mode-trigger`) is the only entry point into editing mode. Verified live on `pnpm dev` (clean localStorage: owner=true, 6/6 draft cards clickable, zero dock/pill/EDIT CONTENT/picker DOM; after the trigger: all visible; after exiting: all hidden again but cards still clickable) and on `vite preview` of a real `pnpm build` (forcing `?owner=1` plus both storage keys to `"1"`: owner and editingMode both stayed hard-`false`, and `data-edit-mode-trigger`/`data-production-export-dock`/`data-owner-editor-dock` are fully absent from the built JS, not just runtime-hidden). `pnpm typecheck` and `pnpm build` pass. See `PROJECT_STATUS.md`'s "Owner permission vs. Editing Mode" section.
- **Mid-task recovery note:** while syncing this round's edited `WorkPage.tsx` from this session's git worktree back to the main checkout, an out-of-sync worktree copy briefly overwrote main's current portfolio-track feature (`getPortfolioTrackLabel`/`PORTFOLIO_TRACK_OPTIONS`, the "all" track filter, `PortfolioTrackTabs`). Caught immediately by `pnpm typecheck` failing; reconstructed the correct current API surface from a same-session `dist/assets/WorkPage-*.js` production build (built earlier in this session, before the mistake) and from `src/lib/projectMetadata.ts`/`src/components/PortfolioTrackTabs.tsx`'s current, untouched exports, then reapplied the intended `editingMode` fix on top. Re-verified with `pnpm typecheck`, `pnpm build`, and live `/work` testing (track tabs, per-row track select, EDIT PROJECT INFO). No commit exists to recover from directly (this session's work is intentionally kept uncommitted); this note exists so the general risk is visible if anything about `/work`'s portfolio-track behavior looks off in a later session.

- **Dual Viewpoint Analysis + local SVG icon library (resolved 2026-09-07).** The registered analysis template now uses one 2-5-card data model with enforced editor limits and deterministic 2, 3, 4, and 5-card responsive compositions. Cards share the same label/icon/concept/description/conclusion structure and retain generated-icon preview, save, replace, and regenerate controls. Legacy paired-column data remains read-compatible and converts on edit. Existing `direction-compare` instances remain unchanged; no external AI provider or dependency was introduced.

- **Game Experience concise evidence guidance and card elevation (resolved 2026-08-11).** AI import keeps future strengths/limitations to the 1-2 strongest observations near 50-80 Chinese characters and moves supporting reasoning into detail without inventing or destructively truncating content. Existing records remain untouched. The main card now uses a softly translucent dark-blue surface; the blue-green takeaway and cover form the raised second plane. Evidence is unboxed 15px/400 support text and detail recedes to 14px. Real Hogwarts Legacy verification confirmed all three planes, natural masonry height, and zero horizontal overflow. `pnpm typecheck` and `pnpm build` pass.

- **Game Experience card information hierarchy (resolved 2026-08-11).** Preserved the two-column masonry and every full text field while giving the card a cohesive editorial flow: 30px identity title, muted 15px context, one primary 19px/600 takeaway surface, unboxed 16px/500 analytical evidence, and secondary 15px detailed reading. Strengths/limitations become two text columns with only a subtle divider at wide desktop cards; the takeaway is the only differentiated inner surface. Real Stardew Valley verification confirmed natural height and no overflow. `pnpm typecheck` and `pnpm build` pass.

- **Game Experience Draft edit/delete CRUD (resolved 2026-08-11).** The existing editor now portals to `document.body`, fixing the apparent dead pencil action for records near the bottom of the scrollable manager. Every Draft/Hidden/Public row has a visible delete action with a title-specific confirmation; it removes exactly one stable record ID, persists immediately, and preserves cover files. Real acceptance used and removed one disposable Draft: edit/save/reopen succeeded, delete survived reload, and the original 13 records were untouched. `pnpm typecheck` and `pnpm build` pass.

- **Game Experience two-column masonry archive (resolved 2026-08-11).** `/play` now renders independent, content-height cards in exactly two CSS masonry columns at desktop widths and one column below `lg`. Covers use a compact horizontal header; every non-empty reflection and detail field is visible without a disclosure button, clamp, ellipsis, or hidden body overflow. Real-page verification at 1440px found two column positions and varied 316/384/410px card heights; 900px produced one column. Stardew Valley, Baldur's Gate 3, Assassin's Creed Shadows, and a shorter record were all present and unclipped. `pnpm typecheck` and `pnpm build` pass.

- **Portfolio export page -> DILIDA Local Deliverables handoff (resolved 2026-08-11).** Targeted Collection PDF and selected single-file HTML now ask for a real Resume Library direction before export; complete-offline HTML uses the global slot without asking. Successful results are handed to DILIDA's existing canonical artifact service, occupied slots require explicit replacement, and the result panel exposes the canonical file actions and real path. Real `/zh/export` acceptance archived a 9-page Collection PDF into `UX / UE / 作品集 / PDF` and a 43.4 MB complete-offline HTML into `完整离线作品集 / HTML`; each final slot contained exactly one file and both records are present in DILIDA's canonical SQLite registry.

- **Publishing preflight blocked on all 13 Game Experience covers, resolved and verified (2026-08-10).** `game-experience-covers`' collector (`src/lib/productionBundleExport.ts`) only sourced cover bytes from the IndexedDB blob store; once a cover is published, the live site renders it from `presentation.coverPublicPath` instead (`useGameCover()`), so a browser whose IndexedDB blob was never re-populated since the last publish reported every such cover as genuinely missing even though the file was already sitting in `public/images/published/game-covers/`. Added a fallback (`fetchAlreadyPublishedGameCover`) that only reuses an already-published file when its path is the exact canonical path for that asset id. Verified by running the real `EXPORT FOR PUBLISH` action and feeding the real captured bundle through the actual `buildPublishingPreflight()` (the function `pnpm portfolio:preflight` uses): `game-experience-covers` now shows `status: "passed"`, zero issues. See `CHANGELOG.md` for the full account.

- **Game Jam projects (`game-jam-8lzejf`, `3d-vdr4qg`) fully resolved and verified live in production (2026-08-08, commit `12e7fdd`).** Missing covers, local playable-game source, duplicated local WebGL builds, and publishing blockers all fixed — see `CHANGELOG.md` for the full account. **Verified on the real production site**, `https://myprofile-teal.vercel.app` (not `myprofile.vercel.app`, which is an unrelated third party's site — do not use it again): both projects show their real cover on the homepage overview and project page, no placeholder; the "打开游戏 / Play Game" link's `href` matches the exact expected Unity Play URL for each project (`877cb160-3478-4f23-bf1d-384942b132f1/gamescript` and `d21db0be-36a7-4193-a21f-64e34a979196/tem`); no iframe/recursion; no console errors on either page. Permanent rule recorded in `PROJECT_STATUS.md`: Playable Game has one canonical hosted build — when a stable external Unity Play URL exists, the portfolio references it via `playUrl` and does not maintain a duplicate publishable local WebGL build.
- **Temporary "HR delivery mode: website-slice (A4 landscape)" export mode retired (2026-08-08).** Removed as obsolete/emergency-only, per explicit request. Removed: the checkbox/state in `PortfolioPdfBuilderPage.tsx`; the `websiteSlice` param, query-param, and emergency plain-text cover builder in `src/lib/portfolioCollectionExport.ts`; the `websiteSlice` plumbing in `scripts/portfolioCollectionExportPlugin.ts`; the `"a4-landscape-slices"` mode and `renderA4LandscapeSlices` in `scripts/exactWebExportPlugin.ts` (canonical `continuous`/`section-pages` modes untouched); `isWebsiteSliceExportCapture()` in `collectionExportStaging.ts`; and its special-casing in `FigmaPrototypeTemplate.tsx`. Also dropped the retired mode entry from `pdfExportRegistry.json`. Verified: `pnpm typecheck`/`pnpm build` pass, zero remaining repo-wide references, live `/en/export` check (no control, checkbox count 7→6, no console errors), and a real regenerated Collection PDF (`ai-assisted-ui-environment-design`, which exercises both `phase-milestones` and `figma-prototype`) opened and inspected — correct content, no errors. See `CHANGELOG.md` for the full file-by-file list.

## Maintenance follow-up after Phase 1

- Run a separate deletion review for `PortfolioPdfDocument.tsx`, `projectPdfContent.ts`, `projectPdfCompositions.ts` and `useLegacyProjectAsset.ts`. Their final shared helper has been extracted and no active imports remain, but no source deletion was performed in Phase 2.
- Decide whether to delete or retain the six legacy template source files now that their stale PDF registry entries are gone. They are unregistered and have no current/historical content instances, but remain NEEDS REVIEW until that decision is explicit.
- Review the 138 unmapped source/public asset files project by project. They remain protected and must not be bulk-deleted.
- Decide separately whether the dev-only migration UI with zero adapters remains as reusable infrastructure.

## Resolved 2026-08-14

- **Collection PDF Figma fallback, independent page chrome, and return-to-index navigation.** The final user-facing `/zh/export` artifacts visibly contain the existing Figma fallback without constructing an iframe in PDF mode. Every selected project receives the same Collection-owned chrome derived from its existing summary, and each return link is a real PDF internal link. Three four-project exports verified equal-width continuous pages, 32px separation, 96px chrome, and 24px final margins with no project-specific branch.
