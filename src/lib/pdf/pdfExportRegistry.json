{
  "version": 1,
  "notes": "Authoritative registry of every content family the Portfolio Collection PDF pipeline can emit. Mirrors src/lib/publishing/publishSourceRegistry.json's role for the publishing pipeline. Read by scripts/pdf-export-preflight-lib.mjs and scripts/pdf-export-postflight-lib.mjs. See docs/PDF_EXPORT_ARCHITECTURE.md for the human-readable version of every field below.",
  "canonicalRendererInvariant": "Project rendering has one canonical source: exact-web export. Collection PDF assembles canonical project output and must not maintain an independent project rendering pipeline. See docs/PDF_EXPORT_ARCHITECTURE.md.",
  "renderers": {
    "captureProjectPage": {
      "implementedIn": "scripts/portfolioCollectionExportPlugin.ts",
      "role": "Shared Collection orchestrator: navigates to the canonical project DOM, audits content/media, delegates project-content printing to renderExactWebPdf, then composes one independent Collection page-chrome layer below that content. It never selects alternate PDFs or project-specific DOM.",
      "usedBy": ["dynamic-project", "ui-personal-practice"]
    },
    "renderExactWebPdf": {
      "implementedIn": "scripts/exactWebExportPlugin.ts",
      "domSnapshotBuiltBy": "buildExactSnapshotResult, src/components/ProjectExactWebExportAction.tsx (exposed dev-only on window.__exactWebExport for captureProjectPage to call)",
      "sizing": "variable-height",
      "captureWidthPx": 1440,
      "modes": {
        "continuous": "single page sized to exact measured content height, used when height <= 7100px",
        "section-pages": "splits into real top-level-section physical pages, used above the continuous-mode threshold"
      },
      "usedBy": ["dynamic-project", "ui-personal-practice", "single-project 'Export Exact Web PDF' button"],
      "note": "The single renderer for project CONTENT in both single-project export and Collection. PROJECT END, summary, selected-order indicator and Back to Index are not inserted into this DOM; Collection adds them afterward as one shared page-level chrome layer."
    },
    "collectionPageChrome": {
      "implementedIn": "scripts/portfolioCollectionExportPlugin.ts",
      "sizing": "fixed-height layer appended to each variable-height project page",
      "widthPx": 1440,
      "heightPx": 96,
      "contentSeparationPx": 32,
      "finalMarginPx": 24,
      "usedBy": ["dynamic-project", "ui-personal-practice"],
      "note": "Independent PDF page furniture only. It spans page-level safe margins, carries PROJECT END / canonical summary / selected-order indicator / Back to Index, and never participates in project DOM measurement."
    },
    "renderSectionPdf": {
      "implementedIn": "scripts/portfolioCollectionExportPlugin.ts",
      "sizing": "fixed",
      "widthPx": 1440,
      "heightPx": 900,
      "usedBy": ["ui-works", "game-experience", "contact"]
    },
    "captureCoverPage": {
      "implementedIn": "scripts/portfolioCollectionExportPlugin.ts",
      "sizing": "fixed",
      "widthPx": 1440,
      "heightPx": 900,
      "usedBy": ["cover", "toc"]
    }
  },
  "sharedGeometry": {
    "source": "src/lib/collectionCoverGeometry.ts",
    "pageWidthPx": 1440,
    "pageHeightPx": 900,
    "horizontalSafeArea": { "leftPx": 80, "rightPx": 1360, "widthPx": 1280 },
    "coverSafeArea": { "topPx": 60, "bottomPx": 870 },
    "note": "Cover (page 1, identity only) and index (page 2, editorial contents list) were split into two separate pages on 2026-08-09 — there is no longer a dot/label TOC grid on the cover. The index page's own column grid (heading, per-column number/title/metadata/thumbnail, equal-width columns, vertical separators) is computed by indexColumnPositions() against INDEX_PAGE in src/lib/collectionCoverGeometry.ts — see that file rather than a duplicated constant table here, since duplicating the numbers here would drift."
  },
  "knownEngineQuirks": [
    {
      "id": "print-media-always-evaluated",
      "description": "Playwright's page.pdf() always evaluates @media print rules in the loaded stylesheet, regardless of any prior page.emulateMedia({ media: 'screen' }) call. emulateMedia only affects on-screen page.evaluate()-based measurement, never what page.pdf() itself resolves stylesheet media queries against.",
      "implication": "Any @media print rule anywhere in src/styles.css is live for every page.pdf() call in this pipeline (captureProjectPage, renderSectionPdf, captureCoverPage), independent of which attribute or query param a rule appears to be scoped by.",
      "mitigation": "preferCSSPageSize: false is set on every page.pdf() call in scripts/portfolioCollectionExportPlugin.ts to defeat unscoped @page rules. Any new @media print rule must be audited against every renderer above, not just the feature that motivated it."
    },
    {
      "id": "unscoped-page-rule",
      "description": "src/styles.css defines @page { size: A4 portrait; margin: 8mm; } inside the same @media print block as scoped print rules. @page cannot be attribute-scoped by definition, so it applies to every page.pdf() call unconditionally.",
      "implication": "This rule is currently inert only because preferCSSPageSize is explicitly set to false on every call site. If a future call site omits that option, page geometry silently reverts to A4 portrait with 8mm margins instead of the pipeline's real 1440x900 / segment-height geometry.",
      "status": "known, not yet fixed as of 2026-08-07 — tracked in TASKS.md, deferred pending re-investigation of the figure/table/img blank-segment symptom through this registry rather than as an isolated patch"
    }
  ],
  "sources": [
    {
      "id": "cover",
      "family": "cover",
      "sourceOfTruth": "Selected project/section list assembled client-side in src/lib/portfolioCollectionExport.ts, geometry from src/lib/collectionCoverGeometry.ts",
      "renderer": "captureCoverPage",
      "sizing": "fixed",
      "width": 1440,
      "height": 700,
      "note": "height changed 900->700 (2026-08-09) to end shortly after the identity graphic/title/footer instead of leaving a large empty area — a fixed constant since cover content never depends on the selection.",
      "horizontalSafeArea": { "left": 80, "right": 1360 },
      "padding": { "top": 60, "bottom": 30 },
      "imageHandling": "none — cover graphic is vector SVG, no raster photography embedded, no project thumbnails (moved to the index page, 2026-08-09)",
      "emptyContentBehavior": "cover always renders; brandLine/footerLabel are static strings, not user content",
      "segmentation": "single physical page (page 1 of the cover+index section), no pagination",
      "verificationRules": ["panel/circle geometry matches collectionCoverGeometry.ts constants", "cover page itself has zero link annotations — all click targets live on the index page"]
    },
    {
      "id": "toc",
      "family": "table-of-contents",
      "sourceOfTruth": "Same selection as cover — entries assembled in src/lib/portfolioCollectionExport.ts (title/coverUrl/metaLabel), column geometry from INDEX_PAGE in src/lib/collectionCoverGeometry.ts",
      "renderer": "captureCoverPage",
      "sizing": "content-driven",
      "width": 1440,
      "height": "computeIndexPageHeight(count) — top margin + actual row content + a modest bottom margin, e.g. 376px for the real 3-project case; never a fixed canvas",
      "note": "No heading (removed 2026-08-09 — the page is just the project entries). Thumbnail aspect 4.3:1->3.2:1 (2026-08-09), within the 3:1-3.5:1 range, so the cover image is actually legible rather than a shallow strip.",
      "horizontalSafeArea": { "left": 80, "right": 1360 },
      "padding": { "top": 0, "bottom": 0 },
      "imageHandling": "each project's own resolved cover image, cropped (never squashed, preserveAspectRatio=xMidYMid slice) to a ~3.2:1 thumbnail per column — same source image as elsewhere, no separate re-encoding pass",
      "emptyContentBehavior": "an entry with no coverUrl (non-project section entries) falls back to a plain panel-toned rect instead of a placeholder/X — its column, title, and click target still work",
      "segmentation": "a separate physical page (page 2 of the cover+index section, immediately after the cover) — no longer part of the cover page (changed 2026-08-09)",
      "verificationRules": [
        "column count equals selected entry count; columns are equal-width and fill horizontalSafeArea exactly",
        "every column's clickable navRect (number through thumbnail bottom) resolves to the correct target page",
        "vertical separators sit only between columns in the same row"
      ]
    },
    {
      "id": "dynamic-project",
      "family": "project-page",
      "sourceOfTruth": "Owner's real draft for the project (localStorage/IndexedDB in DEV, or the project's published draft) resolved through the same staged job payload used by DynamicProjectPage.tsx",
      "renderer": "captureProjectPage",
      "sizing": "variable-height",
      "width": 1440,
      "height": "variable: bottom-most visible rendered project-content bound + 32px separation + 96px Collection page chrome + 24px final margin",
      "horizontalSafeArea": { "left": 0, "right": 1440, "note": "safe area is enforced per-template via each template's own horizontalInset, not a page-level constant like the cover/section pages" },
      "padding": { "top": "template-defined", "bottom": "intendedBottomPadding (see diagnostics)" },
      "imageHandling": "each template-instance image resolved by imageId/localImageId to a data URL before capture; missing images are hard media-slot failures, not silently skipped",
      "emptyContentBehavior": "an empty template wrapper (layoutAudit.emptyTemplateWrappers) is a hard-fail, not rendered blank",
      "segmentation": "one continuous physical PDF page per project; exact-web prints only canonical project content through its measured bottom plus 32px separation, then Collection appends the shared 96px page chrome and 24px final margin; historical generated project PDFs are never selected as capture inputs",
      "verificationRules": [
        "measuredContentBottom must be >100px and finalPageHeight must equal measuredContentBottom + 32px + 96px + 24px",
        "mediaSlotAudit.failed must be 0",
        "every templateFitAudit entry's overflowAfterFit must be 0",
        "layoutAudit.emptyTemplateWrappers must be empty",
        "trailingBlankHeight must equal the final page margin (24px); projectContentSeparation must equal 32px; collectionPageChromeHeight must equal 96px",
        "captureWidth must equal the canonical Collection capture width and overflowRight must be 0",
        "exactWebMode must be continuous and exactWebPdfPages must equal 1",
        "PROJECT END must exist only in the shared Collection page-chrome layer, projectNumber/projectCount must follow selected order, and collectionBackLinkRect must target the Collection index page"
      ]
    },
    {
      "id": "ui-personal-practice",
      "family": "project-page",
      "sourceOfTruth": "src/data/uiPracticeMetadata.json + bundled images under src/assets/ui-practice-optimized (source-controlled, not a browser draft)",
      "renderer": "captureProjectPage",
      "sizing": "variable-height",
      "width": 1440,
      "height": "variable, same content bound + 32px separation + 96px page chrome + 24px final margin as dynamic-project",
      "horizontalSafeArea": { "left": 0, "right": 1440 },
      "padding": { "top": "template-defined", "bottom": "intendedBottomPadding" },
      "imageHandling": "images are pre-published, source-controlled paths — no localStorage/IndexedDB/blob resolution step, unlike ordinary dynamic projects",
      "emptyContentBehavior": "same hard-fail rules as dynamic-project",
      "segmentation": "identical one-continuous-page mechanism to dynamic-project",
      "verificationRules": [
        "same seven rules as dynamic-project",
        "the shared page chrome must remain vector/selectable and independent of the long project-content snapshot"
      ]
    },
    {
      "id": "ui-works",
      "family": "fixed-section",
      "sourceOfTruth": "UiPracticeCatalogItem[] passed into buildUiWorksSectionsHtml (src/lib/portfolioCollectionExport.ts), one section page per UI_WORKS_PER_PAGE items",
      "renderer": "renderSectionPdf",
      "sizing": "fixed",
      "width": 1440,
      "height": 900,
      "horizontalSafeArea": { "left": 80, "right": 1360 },
      "padding": { "top": 60, "bottom": 54 },
      "imageHandling": "every grid image is downscaled to a JPEG data URL (UI_WORKS_CARD_MAX_WIDTH x UI_WORKS_CARD_MAX_HEIGHT) client-side before staging — never embeds the original source asset",
      "emptyContentBehavior": "buildUiWorksSectionsHtml returns [] and the section is omitted entirely when items.length is 0 — never renders an empty page",
      "segmentation": "one physical page per HTML snapshot; multi-page pagination is done by the client pre-splitting items into pages, not by page.pdf() auto-pagination",
      "verificationRules": ["page count matches ceil(itemCount / UI_WORKS_PER_PAGE)", "no card image exceeds its downscale target dimensions"]
    },
    {
      "id": "game-experience",
      "family": "fixed-section",
      "sourceOfTruth": "GameExperienceRecord[] from getGameExperienceStore(), filtered to selection.selectedGameIds",
      "renderer": "renderSectionPdf",
      "sizing": "fixed",
      "width": 1440,
      "height": 900,
      "horizontalSafeArea": { "left": 80, "right": 1360 },
      "padding": { "top": 60, "bottom": 54 },
      "imageHandling": "each game cover resolved via resolveGameCoverSrc (IndexedDB blob by coverAssetId, falling back to record.presentation.coverPublicPath), then downscaled to a 160x160 JPEG data URL — 2.5x the 64x64 CSS display size",
      "emptyContentBehavior": "buildGameExperienceSectionsHtml returns [] and the section is omitted when records.length is 0; a game with no resolvable cover renders an empty placeholder div, not a missing-image failure",
      "segmentation": "one physical page per HTML snapshot, client pre-split into pages of GAMES_PER_PAGE=5 rows each",
      "verificationRules": ["page count matches ceil(selectedGameCount / 5)", "cover images are <=160x160"]
    },
    {
      "id": "contact",
      "family": "fixed-section",
      "sourceOfTruth": "portfolioProfile.contact (email, location) — static profile data, not a draft",
      "renderer": "renderSectionPdf",
      "sizing": "fixed",
      "width": 1440,
      "height": 900,
      "horizontalSafeArea": { "left": 80, "right": 1360 },
      "padding": { "top": 0, "bottom": 0 },
      "imageHandling": "none — text-only page",
      "emptyContentBehavior": "always renders; contact section has no empty state",
      "segmentation": "single physical page, always exactly one page",
      "verificationRules": ["contactLine is non-empty"]
    },
    {
      "id": "dynamic-template-images",
      "family": "image-asset",
      "sourceOfTruth": "Owner draft image (localStorage/IndexedDB blob) resolved by imageId/localImageId, staged as a data URL into the job payload before capture",
      "publishingRegistryAdapterId": "dynamic-template-images",
      "renderer": "consumed by captureProjectPage, not independently rendered",
      "imageHandling": "resolved and embedded per template instance; see each template's row below for per-template image slot behavior",
      "emptyContentBehavior": "an unresolved imageId is a media-slot failure (mediaSlotAudit), not a silently blank slot",
      "verificationRules": ["every referenced imageId in the staged draft has a corresponding entry in assets-manifest.json"]
    },
    {
      "id": "ui-practice-images",
      "family": "image-asset",
      "sourceOfTruth": "src/assets/ui-practice-optimized, referenced by src/data/uiPracticeMetadata.json — source-controlled, not a browser draft",
      "publishingRegistryAdapterId": "ui-practice-images",
      "renderer": "consumed by captureProjectPage (ui-personal-practice project) and renderSectionPdf (ui-works section)",
      "imageHandling": "already-published static paths; no blob/localStorage resolution",
      "emptyContentBehavior": "a missing referenced file is a build-time/dev-server 404, not a pipeline-level fallback",
      "verificationRules": ["referenced path exists on disk before capture"]
    },
    {
      "id": "project-covers",
      "family": "image-asset",
      "sourceOfTruth": "Project cover IndexedDB store (owner draft) or published cover path",
      "renderer": "not directly embedded in the Collection PDF today — used by the website/cover editor and the publishing pipeline, not by captureProjectPage/renderSectionPdf",
      "imageHandling": "n/a to the PDF pipeline",
      "emptyContentBehavior": "n/a to the PDF pipeline",
      "verificationRules": ["confirm during any future PDF feature work whether project covers become part of a project page before assuming this row is dormant"],
      "status": "not currently consumed by the Collection PDF — listed for completeness per the requested content-family coverage, not because it is live in this pipeline"
    },
    {
      "id": "figma-embeds",
      "family": "template-external-preview",
      "sourceOfTruth": "Figma prototype/showcase URLs stored in the template instance content",
      "renderer": "captureProjectPage (as part of whichever project contains the template instance)",
      "imageHandling": "The explicit exportMode is the only switch: live tries the iframe and falls back only on genuine failure; pdf/offline never instantiate an iframe and immediately render the canonical fallback image in the same media frame. Capture requires that fallback to decode with non-zero natural and visible dimensions.",
      "emptyContentBehavior": "a configured fallback that cannot decode is a hard capture failure; an instance with no configured fallback keeps the template's existing failure behavior",
      "verificationRules": ["every configured [data-figma-prototype-frame=\"fallback\"] contains a decoded, visibly sized img[data-figma-prototype-fallback] before page.pdf() runs", "the final merged Collection PDF must visibly show the fallback image, not merely contain a successful asset response"]
    },
    {
      "id": "playable-game-embeds",
      "family": "template-external-preview",
      "sourceOfTruth": "Playable Game build entry (see publishing registry's playable-game-builds/playable-game-covers adapters)",
      "renderer": "captureProjectPage",
      "imageHandling": "not yet independently verified in this session which fallback (poster, blank, or attempted iframe capture) PlayableGameTemplate produces under page.pdf(); flagged as an open verification gap, not a confirmed behavior",
      "emptyContentBehavior": "unverified — do not assume parity with the Figma print-swap pattern until PlayableGameTemplate's print/capture branch is actually read and confirmed",
      "verificationRules": ["confirm PlayableGameTemplate's PDF-capture behavior before relying on this row for any future fix"],
      "status": "open verification gap, tracked in TASKS.md alongside the broader Playable Game production-hosting gap"
    }
  ],
  "templates": [
    { "id": "project-header", "file": "src/templates/ProjectHeaderTemplate.tsx", "pdfBehavior": "standard — renders as captured, no confirmed special print/PDF branch" },
    { "id": "statement-longform", "file": "src/templates/StatementLongformTemplate.tsx", "pdfBehavior": "standard" },
    { "id": "phase-milestones", "file": "src/templates/PhaseMilestonesTemplate.tsx", "pdfBehavior": "has a dedicated export fit rule in captureProjectPage's templateFitAudit (see TASKS #106 history) — treat as having confirmed special handling, not yet fully re-documented here" },
    { "id": "process-flow", "file": "src/templates/ProcessFlowTemplate.tsx", "pdfBehavior": "wide-content template with a scoped fit-scale applied only in collection-export capture mode to avoid horizontal clipping" },
    { "id": "image-row", "file": "src/templates/ImageRowTemplate.tsx", "pdfBehavior": "standard — image slots resolved via the dynamic-template-images source row above" },
    { "id": "direction-compare", "file": "src/templates/DirectionCompareTemplate.tsx", "pdfBehavior": "standard — image slots resolved via the dynamic-template-images source row above" },
    { "id": "decision-table", "file": "src/templates/DecisionTableTemplate.tsx", "pdfBehavior": "standard" },
    { "id": "circle-summary", "file": "src/templates/CircleSummaryTemplate.tsx", "pdfBehavior": "standard" },
    { "id": "supporting-note", "file": "src/templates/SupportingNoteTemplate.tsx", "pdfBehavior": "standard" },
    { "id": "xmind-breakdown", "file": "src/templates/XMindBreakdownTemplate.tsx", "pdfBehavior": "single/double display-mode vector diagram; single-mode layout was fixed and verified 2026-08-07 (see CHANGELOG)" },
    { "id": "figma-prototype", "file": "src/templates/FigmaPrototypeTemplate.tsx", "pdfBehavior": "collection-capture fallback-image branch with mandatory decode/visible-size guard; see figma-embeds source row above" },
    { "id": "figma-showcase", "file": "src/templates/FigmaShowcaseTemplate.tsx", "pdfBehavior": "not yet independently confirmed whether it shares FigmaPrototypeTemplate's print-swap markup or has its own — treat as unverified until read" },
    { "id": "playable-game", "file": "src/templates/PlayableGameTemplate.tsx", "pdfBehavior": "see playable-game-embeds source row above — open verification gap" }
  ]
}
